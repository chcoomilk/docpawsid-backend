import { VaccineHistory } from "@prisma/client";

export const validate_vaccine_input = (history: VaccineHistory): string | null => {
  // const  = Date.parse(String(history.date_administered));
  // if ()
  return null;
};
